version: '3.7'

services:
  db:
    image: arm64v8/mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: 12345678
      MYSQL_DATABASE: test
    volumes:
      - ~/docker_data/mysql:/var/lib/mysql
    ports:
      - "3308:3306"


